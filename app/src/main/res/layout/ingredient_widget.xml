<LinearLayout
    android:id="@+id/ll_widget_view"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:orientation="vertical"
    android:padding="@dimen/standard_padding">

    <TextView
        android:id="@+id/tv_widget_recipe_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:contentDescription="@string/widget_name_label"
        android:textSize="@dimen/recipe_font"
        android:textStyle="bold"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/widget_ingredients_need_label"/>

    <TextView
        android:id="@+id/tv_widget_ingredient_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/standard_padding"
        android:contentDescription="@string/widget_description"
        android:textSize="@dimen/standard_font"
        android:textStyle="bold|italic"/>

</LinearLayout>